<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Drink App</title>
    <style type="text/css">
        body {
          color: blanchedalmond;
          background-color: steelblue;
        }
      </style>
      
</head>
<body>
    <h1>Welcome to gitPub</h1>

    <% for (let food in allFoods) { %>
         <li>
             <!-- https://flexiple.com/javascript-capitalize-first-letter/ -->
             <a href="/foods/<%=food%>"><%=allFoods[food].name.charAt(0).toUpperCase() + allFoods[food].name.slice(1) %></a>
             
         </li>
    <% } %>
</body>
</html>